apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-golang-chart.fullname" . }}
  labels:
    {{- include "my-golang-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-golang-chart.selectorLabels" . | nindent 4 }}
